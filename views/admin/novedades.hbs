<div class="container" style="margin-top: 50px;">
    {{!-- fila 1 --}}
    <div class="row">
        <div class="col">
            <p class="text-end">
                <span>Hola {{usuario}}</span>
                <a href="/admin/login/logout" class="btn btn-danger">Cerrar sesión <i
                        class="fas fa-sing-out-alt"></i></a>
            </p>
        </div>
    </div>
    {{!-- fin fila 1 --}}

    {{!-- fila 2 --}}

    <div class="row">
        <div class="col">
            <h3>Lista de novedades</h3>
        </div>
        <div class="col-2 text-end">
            <a href="#" class="btn btn-success"> <i class="fw-normal">Nuevo</i></a>
        </div>
    </div>

    {{!-- fila 3 --}}
    <table class="table table-striped table-hover" style="padding:300px;">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">{{titulo}}</th>
                <th scope="col" class="text-end">Acciones</th>
            </tr>
        </thead>
        <tbody>
            {{#each novedades}}
            <tr>
                <th scope="row">{{id}}</th>
                <td>{{titulo}}</td>
                <td class="text-end">
                    <a href="#" class="btn btn-sm btn-secondary"><i class="fa fa-pencil"></i></a>
                    <a href="/admin/novedades/eliminar/{{id}}" class="btn btn-sm btn-secondary"><i class="fa fa-trash"></i></a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    {{!-- fin fila 2 --}}
</div>